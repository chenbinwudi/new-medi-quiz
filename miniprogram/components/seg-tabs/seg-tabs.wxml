<view class="tabs">
  <view
    wx:for="{{renderTabs}}"
    wx:key="value"
    data-value="{{item.value}}"
    class="tab {{item.activeClass}}"
    bindtap="onTap"
  >
    {{item.label}}
  </view>
</view>
