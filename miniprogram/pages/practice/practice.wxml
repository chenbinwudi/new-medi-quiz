<view class="practice-page">
  <view class="practice-head">
    <view>
      <view class="title">章节练习</view>
      <view class="badge">{{typeLabel}}</view>
    </view>
    <view class="muted">{{currentIndex + 1}}/{{questions.length}}</view>
  </view>

  <view class="question-card">
    <view class="stem">{{current.stem}}</view>
    <view class="options">
      <view
        wx:for="{{current.options}}"
        wx:key="key"
        data-key="{{item.key}}"
        bindtap="selectOption"
        class="option {{item.selected ? 'selected' : ''}} {{submitted && item.isAnswer ? 'correct' : ''}}"
      >
        <view class="letter">{{item.key}}</view>
        <view class="option-text">{{item.text}}</view>
      </view>
    </view>
    <button wx:if="{{!submitted}}" class="submit-btn" bindtap="submit">提交答案</button>
  </view>

  <view wx:if="{{submitted}}" class="answer-card">
    <view class="answer-title {{isCorrect ? 'ok' : 'bad'}}">{{isCorrect ? '回答正确' : '回答错误'}}</view>
    <view class="answer-row">
      <text>正确答案</text>
      <text class="red">{{correctAnswerText}}</text>
    </view>
    <view class="analysis">{{current.analysis}}</view>
  </view>

  <view class="bottom-bar">
    <view class="tool" bindtap="toggleFavorite">{{favorited ? '已收藏' : '收藏'}}</view>
    <view class="tool" bindtap="saveNote">笔记</view>
    <view class="tool" bindtap="showCard">答题卡</view>
    <button class="nav-btn ghost" bindtap="prev">上一题</button>
    <button class="nav-btn" bindtap="next">下一题</button>
  </view>
</view>
